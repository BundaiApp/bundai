import { View } from 'react-native'

export const VerticalSpacer = ({ height }) => <View style={{ height: `${height}%` }} />
export const HorizontalSpacer = ({ width }) => <View style={{ width: `${width}%` }} />
